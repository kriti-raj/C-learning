// Progam to find area of a rectangle

# include<stdio.h>

    int main(){

        int l, b;
        printf("Enter the size of Length \n");
        scanf("%d", &l);

        printf("Enter the size of Breath \n");
        scanf("%d", &b);

        printf("Area of the rectangle is %d", l*b);
    return 0;
}