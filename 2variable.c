// Variable learning

#include <stdio.h>

int main()
{
    int a = 4;
    // int b = 9.8; // Not recommended because 9.8 is not an integer

    float b = 8.5;
    char c = 'u';
    printf("The value of a is %d \n", a);

    printf("The value of a is %f \n", b);

    printf("The value of a is %c \n", c);

    return 0;
}